<section class="container">
    <h3 class="post-name">{{post.name}}</h3>
    <section class="flex-row justify-center">
        {{#each post as |post|}}
        {{> post-details}}
        {{/each}}
    </section>
</section>
<button type="submit" class="btn"><a class="link" href="/dashboard">+ New Post</a></button>
