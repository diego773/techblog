<section class="container">
    <h3 class="post-name">{{post.name}}</h3>
    <h4>Opening Hour: {{format_time post.info_date}}</h4>
    <section class="flex-row justify-center">
        {{#each post as |post|}}
        {{> post-details}}
        {{/each}}
    </section>
</section>
<button type="submit" class="btn"><a class="link" href="/dashboard/new">+ New Post</a></button>

<script src="/public/js/addPost.js"></script>